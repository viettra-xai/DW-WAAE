import os
import sys

# Ensure the src directory is in sys.path for correct module imports
sys.path.append(os.path.abspath(os.path.dirname(__file__)))

# Optional: Print confirmation
print("Initialized the src package.")
